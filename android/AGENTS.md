# Android AGENTS Instructions

- Release builds receive `VERSION_NAME` and `VERSION_CODE` from CI; Android releases publish signed APKs to GitHub Releases for Obtainium. Google Play Publisher is not applied to standalone APK builds.
